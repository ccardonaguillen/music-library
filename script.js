function Record(title, artist, release_year, owned, format="") {
    this.title = title;
    this.artist = artist;
    this.release_year = release_year;
    this.format = format;
    this.owned = owned
}
